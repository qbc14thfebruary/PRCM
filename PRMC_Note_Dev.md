Ghi chú cuối cùng cho Developer

Không Hardcode: Tuyệt đối không để số 0.9 hay 0.65 trong code chính. Tất cả phải nằm trong hàm tính động.

Logging bắt buộc: Mỗi lần trả về BLOCKED, phải ghi log lý do để tối ưu sau.

Backup Rollback: Nếu phiên bản mới (v2.0) gây lỗi, lưu sẵn tham số mặc định của phiên bản gốc để khôi phục.